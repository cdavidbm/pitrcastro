---
import Base from '../../../../layouts/Base.astro';
import HeroPage from '../../../../components/HeroPage.astro';
import { resolveUrl } from '../../../../utils/url';
import { getObservatorioEjeDeEducacionCuento } from '../../../../utils/strapi-fetchers';
import { mediaUrl } from '../../../../utils/strapi-media';
const pageData: any = (await getObservatorioEjeDeEducacionCuento()) ?? {};
const base = import.meta.env.BASE_URL;
const breadcrumb = [
  { label: 'Inicio', href: base },
  { label: 'Observatorio', href: resolveUrl('/observatorio') },
  { label: 'ITRC para Niños', href: resolveUrl('/observatorio/eje-de-educacion/itrc-para-ninos') },
  { label: pageData.title }
];
const pdfHref = mediaUrl(pageData.pdfUrl);
---

<Base title={pageData.title} description={pageData.description} strapiSlug="observatorio-eje-de-educacion-cuento">
  <HeroPage title={pageData.title} subtitle={pageData.description} icon={pageData.icon} breadcrumb={breadcrumb} />

  <section class="cuento-section">
    <div class="container">
      <div class="cuento-actions">
        <a href={pdfHref} download class="cuento-btn cuento-btn--primary">
          <i class="fa-solid fa-download" aria-hidden="true"></i>
          <span>Descargar PDF</span>
          <small>{pageData.pdfSize} · {pageData.pdfPaginas} páginas</small>
        </a>
        <a href={pdfHref} target="_blank" rel="noopener" class="cuento-btn cuento-btn--secondary">
          <i class="fa-solid fa-up-right-from-square" aria-hidden="true"></i>
          <span>Abrir PDF en pestaña nueva</span>
        </a>
      </div>

      <div class="cuento-embed">
        <iframe
          src={pageData.embedUrl}
          title={pageData.title}
          allowfullscreen
          allow="clipboard-write"
          loading="lazy"
        ></iframe>
      </div>

      <p class="cuento-help">
        <i class="fa-solid fa-circle-info" aria-hidden="true"></i>
        Pasa las páginas tocando o arrastrando, o usa el botón <strong>Descargar PDF</strong> para verlo sin conexión.
      </p>

      <div class="back-link">
        <a href={resolveUrl('/observatorio/eje-de-educacion/itrc-para-ninos')}>
          <i class="fa-solid fa-arrow-left" aria-hidden="true"></i> Volver a ITRC para Niños
        </a>
      </div>
    </div>
  </section>
</Base>

<style>
  .cuento-section { padding: var(--space-8) var(--space-4) var(--space-12); background: var(--theme-surface); }
  .cuento-section .container { max-width: 1100px; margin: 0 auto; }

  .cuento-actions {
    display: flex; flex-wrap: wrap; gap: var(--space-3); justify-content: center;
    margin-bottom: var(--space-6);
  }
  .cuento-btn {
    display: inline-flex; align-items: center; gap: var(--space-2);
    padding: var(--space-3) var(--space-5);
    border-radius: var(--radius-full); text-decoration: none; font-weight: 700;
    font-size: var(--font-size-sm);
    transition: transform var(--transition-fast), box-shadow var(--transition-fast), background var(--transition-fast), color var(--transition-fast);
  }
  .cuento-btn small { font-weight: 500; opacity: 0.85; font-size: var(--font-size-xs); margin-left: var(--space-1); }
  .cuento-btn--primary { background: var(--itrc-gold); color: var(--itrc-navy); box-shadow: var(--shadow-md); }
  .cuento-btn--primary:hover { transform: translateY(-2px); box-shadow: var(--shadow-lg); }
  .cuento-btn--secondary {
    background: transparent; color: var(--theme-heading);
    border: 2px solid var(--theme-border);
  }
  .cuento-btn--secondary:hover { background: var(--theme-surface-alt); border-color: var(--itrc-gold); color: var(--itrc-navy); }

  .cuento-embed {
    position: relative; width: 100%; padding-bottom: 65%;
    background: #000; border-radius: var(--radius-xl);
    overflow: hidden; box-shadow: var(--shadow-xl);
  }
  .cuento-embed iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: 0; }

  .cuento-help {
    margin-top: var(--space-4); text-align: center;
    font-size: var(--font-size-sm); color: var(--theme-text-secondary);
  }
  .cuento-help i { color: var(--itrc-gold); margin-right: var(--space-1); }

  .back-link { margin-top: var(--space-8); text-align: center; }
  .back-link a { display: inline-flex; align-items: center; gap: var(--space-2); color: var(--theme-text-muted); text-decoration: none; font-size: var(--font-size-sm); font-weight: 600; }
  .back-link a:hover { color: var(--itrc-gold); }

  @media (max-width: 768px) {
    .cuento-embed { padding-bottom: 130%; }
    .cuento-btn { width: 100%; justify-content: center; }
  }
</style>
